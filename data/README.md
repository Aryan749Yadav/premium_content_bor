# Data Directory

This directory contains:
1. `database.db` - SQLite database (created automatically)
2. `menu_export.json` - Menu backup/export file

DO NOT commit database.db to version control if it contains real user data.
